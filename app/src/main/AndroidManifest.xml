<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.magomtz.android.cartilla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".petlist.ui.PetListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pet.AddEditPetActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".owner.AddEditOwnerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".vaccine.PetInfoActivity"
            android:label="@string/title_activity_pet_ifno"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".vaccine.addeditvaccine.AddVaccineActivity"
            android:label="@string/title_activity_add_vaccine"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>